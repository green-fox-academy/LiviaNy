// Given a string, compute recursively a new string where all the adjacent chars are now separated by a *

function addStars(string: string) {
  let newStringArr: string[] = string.split(``);
  console.log(newStringArr);
  //     if (){

  // }else {

  // }
}

addStars(`Starfully`);
